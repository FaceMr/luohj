package com.luohj.spring.designer.observer;

public class Observer1 extends NoticeFlag implements Observer {
	@Override
	public void update() {
		if(!flag){
			System.out.println("observer1 has received!");
			setFlag(true);
		}
	}
}
